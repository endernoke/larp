import type { EngagementDefinition, Opportunity, WorkItemDefinition } from '../types';

export const allOpportunities = [] as Opportunity[];
export const allEngagementDefinitions = [] as EngagementDefinition[];
export const allWorkItemDefinitions = [] as WorkItemDefinition[];
